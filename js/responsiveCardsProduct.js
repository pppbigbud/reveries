// function updateCardDisplay() {
//   var screenWidth = window.innerWidth;
//   var cardsPerRow = 4; // Nombre de cartes par rangée par défaut

//   if (screenWidth < 768) {
//     cardsPerRow = 2; // Réduire à 2 cartes par rangée sur les écrans moyens
//   }

//   if (screenWidth < 480) {
//     cardsPerRow = 1; // Réduire à 1 carte par rangée sur les petits écrans
//   }

//   // Mettez à jour le style en fonction du nombre de cartes par rangée
//   var productContainer = document.getElementById("product-container");
//   productContainer.style.columnCount = cardsPerRow;
// }

// // Appelez la fonction pour la première fois au chargement de la page
// updateCardDisplay();

// // Appelez la fonction lors du redimensionnement de la fenêtre
// window.addEventListener("resize", updateCardDisplay);
